@model WebUI.Models.CategoryViewModels.CategoryDetailsViewModel

@{
    ViewBag.Title = "CategoryDetails";
}

<h2>CategoryDetails</h2>

<div>
    @*<h4>CategoryDetailsViewModel</h4>*@
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.CreateDateTime)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.CreateDateTime)
        </dd>

    </dl>

    <p>
        @if (ViewBag.Same == "true")
        {
            @Html.ActionLink("Edit", "EditCategory", new { id = Model.Id })
            <br />
        }
        
        @Html.ActionLink("Back to Category List", "CategoryList", new { id = ViewBag.Previous })
    </p>

    <hr />

    @Html.Action("CategoryPageArticles", new { id = Model.Id })
</div>

